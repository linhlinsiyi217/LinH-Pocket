# LinH Pocket · 项目约定

## 一、部署规则
1. 用户说"上线"时，自动执行：git add . && git commit -m "update" && git push
2. 提交前先 git status 确认无敏感文件（.env / 密钥 / API Key），有则提醒用户
3. commit 信息优先用用户指定的版本号（如"v0.1 基础框架"），未指定则用 update: 日期
4. 推送完成后告知用户：GitHub Pages 需 1~3 分钟构建，完成后可刷新查看
5. 推送前确认 index.html 已保存（无未保存标记）

## 二、代码修改规则
1. 每次改代码必须直接编辑现有文件本身，不要生成新文件（除非用户明确要求）
2. 改完贴出：改动的文件名 + 关键片段，等用户确认后再保存
3. 用户说"确认 / OK / 继续"后才保存并推进下一步
4. 禁止跳步，严格按用户指定的顺序执行

## 三、工作风格（永久生效）
1. 改任何 UI / 交互问题时，主动做审美判断，不只"修 bug"，要顺手优化视觉体验
2. 修复问题时有创造空间：可自行决定微交互细节（过渡动画时长、缓动曲线、阴影层次、圆角微调等），并主动提出优化点
3. 底线：不擅自删功能、不改业务逻辑、不改用户已确认的参数。在这些框架内有充分自由发挥
4. 每次改完除贴出修复内容，额外用一两句说明"顺手优化了什么 + 为什么"

## 四、版本与更新规范
1. 补丁（小修复）属于更新，写进 CHANGELOG，版本号不升级
2. 补丁条目用紫色标签 [补丁]，并写明：改动点 + 影响的文件/功能
3. 正式版本发布时，累积补丁合并进对应版本说明，去掉补丁标记

### 更新日志颜色标记（永久生效）
| 类型 | 颜色 | 含义 | 例子 |
|------|------|------|------|
| 新增 | 🟢 绿色 | 以前没有，现在有了 | 新增相册应用、新增壁纸设置 |
| 修复 | 🔴 红色 | 以前坏了，现在好了 | 修复相册打不开、修复时间不刷新 |
| 调整 | 🔵 蓝色 | 以前能用，现在更好用 | 图标间距调小、动画速度调快 |
| 补丁 | 🟣 紫色 | 小修小调，版本号不升 | 微调圆角、修复小样式问题 |

### 更新通知规范（永久生效）
1. 每次有更新（补丁或大版本），所有用户（新老）必须看到通知，不做区分
2. 通知方式：小红点（醒目、强提醒）
   - 出现在设置入口 / 首页角落 / Dock 栏等明显位置
   - 有更新就亮红点，点击后消失
3. 点击红点 → 弹出更新日志弹窗，展示本次更新内容
   - 弹窗样式：浅灰圆角卡片，彩色标签（绿新增/红修复/蓝调整/紫补丁）
   - 展示完整更新日志（含累积补丁）
4. 已读记录存 localStorage，有新版/新补丁时红点重新出现

## 五、手机端适配原则（通用）
1. 可滑动区域：确保 overflow: auto、-webkit-overflow-scrolling: touch、touch-action: pan-x pan-y，不能有透明覆盖层拦截 touch 事件
2. 父级避免 overflow: hidden 裁掉滑动区域
3. flex 子项滑动时注意 flex-shrink: 0 与明确 min-width
4. 交互优先 touch，桌面端保留 hover，移动端用 click / touchstart 切换状态
5. 浮层/弹窗注意 safe-area-inset 与安全区域

### 六、视觉设计规则（黑白韩系 ins 风 + iOS 拟态）

#### 1. 整体基调
- 风格：Apple 极简克制 + Linear 锐利清晰，主色黑白韩系 ins 风
- 定位：手机模拟器网页应用（小手机），动效微交互，不花哨
- 密度：留白即信息，低密高层级
- 无彩色强调：不设 accent 强调色，层级全部靠黑白灰 / 透明度 / 阴影区分

#### 2. 色彩规范（纯黑白灰）
- 背景：深空灰（#1c1c1e）或纯黑（#000000）
- 文字：纯白 #ffffff；次级文字浅灰 #8e8e93
- 卡片/容器：半透明白 rgba(255,255,255,0.05~0.10)
- 层级手段：白色透明度梯度（0.05 / 0.08 / 0.10 / 0.12）+ 发丝分割线 rgba(255,255,255,0.08)，不靠颜色
- 文本选区：::selection 用 rgba(255,255,255,0.12)，禁止浏览器默认蓝底，保证全页无彩色

#### 3. iOS 拟态
- 毛玻璃：backdrop-filter: blur(20px) saturate(180%)（-webkit- 前缀同步），背景 rgba(255,255,255,0.06)，配合 0.5px 发丝描边 rgba(255,255,255,0.08) 强化边缘
- 双阴影拟态（深色底）：
  - 凸起（.neu）：-6px -6px 12px rgba(255,255,255,0.05), 6px 6px 12px rgba(0,0,0,0.45)
  - 内凹（.neu-inset / 按压态）：inset -6px -6px 12px rgba(255,255,255,0.03), inset 6px 6px 12px rgba(0,0,0,0.45)
  - 拟态元素背景必须与父级同色（#1c1c1e），否则浮不起来
  - 按压反馈（.neu:active）：凸起面切换为内凹阴影 + scale(0.98)

#### 4. 间距与栅格
- 基准：8px 栅格
- 页面内边距：16px / 24px
- 元素间距：8 / 16 / 24 / 32px（倍数递增）
- 手机模拟器比例：保持真实手机宽高比（9:19.5），居中显示

#### 5. 圆角规范
- 小卡片 / 按钮：12px
- 大容器 / 模拟器外框：24px
- 模拟器外屏：40px
- 图标/头像：按尺寸半数圆角或全圆

#### 6. 字体排版
- 中文：苹方（PingFang SC）
- 英文/数字：SF Pro（系统级），回退用 -apple-system
- 字号：标题 20-24px / 正文 14-16px / 辅助 12px
- 字重：常规 400，强调 500/600，不滥用粗体

#### 7. 动效与交互
- 时长：200ms（弹跳类 400ms）
- 标准缓动：cubic-bezier(0.25, 0.1, 0.25, 1)
- 弹跳曲线：cubic-bezier(0.34, 1.56, 0.64, 1)（轻盈回弹，用于模拟器切换、弹性点击 .tap-bounce）
- 微交互：点击态透明度 0.7 + 轻微缩放（0.98），过渡自然
- 模拟器切换：淡入淡出 + 位移 + 轻微弹跳（400ms）
- 必须保留 prefers-reduced-motion 降级：系统开启减弱动态效果时全部过渡/动画降至 0.01ms

#### 8. 文件对应
- 结构：index.html（骨架）+ style.css（视觉）+ app.js（交互）
- 所有视觉值走 style.css 的 :root tokens（CSS 变量），禁止散写色值/阴影
- 所有视觉规范优先在 style.css 实现，app.js 只管逻辑
- 后续若加视觉规范，同步更新本章与 style.css 注释

## 9. 与部署规则协同
- 说“上线”仍走第一章部署流程（git add/commit/push）
- 视觉改动 commit 建议带版本（如“v0.1 视觉基底”）